import { useState, useEffect } from "react";

export default function Alert() {
  // let [moviesCount, setMoviesCount] = useState(0);
  // let [actorsCount, setActorsCount] = useState(0);

  return (
    <div>
      <h2>Count</h2>
      <p>Number of movies:</p>
      <p>Number of actors:</p>
      <p>Total count:</p>
    </div>
  );
}
